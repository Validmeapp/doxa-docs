---
title: "Primeiros Passos"
description: "Guia completo de início rápido para integrar com nossa API de plataforma de identidade digital"
version: "v1"
locale: "pt"
order: 1
tags: ["primeiros-passos", "início-rápido", "tutorial"]
---

# Primeiros Passos

Este guia abrangente ajudará você a integrar com nossa API de plataforma de identidade digital em minutos. Você aprenderá como se autenticar, fazer suas primeiras chamadas de API e implementar fluxos comuns.

## Pré-requisitos

Antes de começar, certifique-se de ter:

- Uma conta de desenvolvedor (cadastre-se em [developer.example.com](https://developer.example.com))
- Conhecimento básico de APIs REST e solicitações HTTP
- Sua linguagem de programação preferida e cliente HTTP

## Passo 1: Obtenha suas Chaves de API

1. **Faça login** no seu painel do desenvolvedor
2. **Navegue** até a seção "Chaves de API"
3. **Clique** em "Gerar Nova Chave"
4. **Copie** sua chave de API e armazene-a com segurança

```bash
# Sua chave de API será parecida com esta:
sk_live_1234567890abcdef1234567890abcdef
```

⚠️ **Importante**: Nunca exponha suas chaves de API no código do lado do cliente ou repositórios públicos.

## Passo 2: Faça sua Primeira Chamada de API

Vamos começar testando sua chave de API com uma solicitação simples para obter informações da sua conta:

```bash
curl -X GET "https://api.example.com/v1/account" \
  -H "Authorization: Bearer sk_live_1234567890abcdef1234567890abcdef" \
  -H "Content-Type: application/json"
```

```javascript
// Usando fetch em JavaScript
const response = await fetch('https://api.example.com/v1/account', {
  headers: {
    'Authorization': 'Bearer sk_live_1234567890abcdef1234567890abcdef',
    'Content-Type': 'application/json'
  }
});

const account = await response.json();
console.log(account);
```

```python
# Usando requests em Python
import requests

headers = {
    'Authorization': 'Bearer sk_live_1234567890abcdef1234567890abcdef',
    'Content-Type': 'application/json'
}

response = requests.get('https://api.example.com/v1/account', headers=headers)
account = response.json()
print(account)
```

### Resposta Esperada

```json
{
  "id": "acc_1234567890",
  "name": "Nome da Sua Empresa",
  "email": "admin@suaempresa.com",
  "plan": "pro",
  "created_at": "2024-01-15T10:30:00Z",
  "limits": {
    "monthly_requests": 100000,
    "used_requests": 1250
  }
}
```

## Passo 3: Crie seu Primeiro Usuário

Agora vamos criar uma conta de usuário na sua aplicação:

```javascript
const userData = {
  email: 'joao.silva@exemplo.com',
  first_name: 'João',
  last_name: 'Silva',
  password: 'SenhaSegura123!',
  send_welcome_email: true
};

const response = await fetch('https://api.example.com/v1/users', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer SUA_CHAVE_API',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify(userData)
});

const user = await response.json();
console.log('Usuário criado:', user);
```

```python
import requests

user_data = {
    'email': 'joao.silva@exemplo.com',
    'first_name': 'João',
    'last_name': 'Silva',
    'password': 'SenhaSegura123!',
    'send_welcome_email': True
}

response = requests.post(
    'https://api.example.com/v1/users',
    headers={
        'Authorization': 'Bearer SUA_CHAVE_API',
        'Content-Type': 'application/json'
    },
    json=user_data
)

user = response.json()
print('Usuário criado:', user)
```

### Resposta Esperada

```json
{
  "id": "usr_0987654321",
  "email": "joao.silva@exemplo.com",
  "first_name": "João",
  "last_name": "Silva",
  "status": "pending",
  "created_at": "2024-01-20T15:30:00Z",
  "verification": {
    "email_verified": false,
    "verification_sent": true
  }
}
```

## Passo 4: Configure Webhooks (Opcional)

Webhooks permitem que você receba notificações em tempo real quando eventos ocorrem. Veja como configurar um webhook básico:

```javascript
const webhookData = {
  url: 'https://seu-app.com/webhooks/identity',
  events: ['user.created', 'user.verified', 'identity.verified'],
  description: 'Webhook principal para eventos de usuário'
};

const response = await fetch('https://api.example.com/v1/webhooks', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer SUA_CHAVE_API',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify(webhookData)
});

const webhook = await response.json();
console.log('Webhook criado:', webhook);
```

### Exemplo de Endpoint de Webhook

```javascript
const express = require('express');
const crypto = require('crypto');
const app = express();

app.use(express.raw({ type: 'application/json' }));

app.post('/webhooks/identity', (req, res) => {
  const signature = req.headers['x-webhook-signature'];
  const payload = req.body;
  
  // Verificar assinatura do webhook
  const expectedSignature = crypto
    .createHmac('sha256', process.env.WEBHOOK_SECRET)
    .update(payload, 'utf8')
    .digest('hex');
  
  if (`sha256=${expectedSignature}` !== signature) {
    return res.status(401).send('Assinatura inválida');
  }
  
  const event = JSON.parse(payload);
  console.log('Evento recebido:', event.type);
  
  // Tratar o evento
  switch (event.type) {
    case 'user.created':
      console.log('Novo usuário:', event.data.user.email);
      break;
    case 'user.verified':
      console.log('Usuário verificado:', event.data.user.email);
      break;
    default:
      console.log('Tipo de evento não tratado:', event.type);
  }
  
  res.status(200).send('OK');
});

app.listen(3000, () => {
  console.log('Servidor de webhook rodando na porta 3000');
});
```

## Passo 5: Trate Erros Graciosamente

Sempre implemente tratamento de erros adequado na sua integração:

```javascript
async function criarUsuario(userData) {
  try {
    const response = await fetch('https://api.example.com/v1/users', {
      method: 'POST',
      headers: {
        'Authorization': 'Bearer SUA_CHAVE_API',
        'Content-Type': 'application/json'
      },
      body: JSON.stringify(userData)
    });
    
    if (!response.ok) {
      const error = await response.json();
      throw new Error(`Erro da API: ${error.error.message}`);
    }
    
    return await response.json();
  } catch (error) {
    console.error('Falha ao criar usuário:', error.message);
    throw error;
  }
}
```

```python
import requests
from requests.exceptions import RequestException

def criar_usuario(user_data):
    try:
        response = requests.post(
            'https://api.example.com/v1/users',
            headers={
                'Authorization': 'Bearer SUA_CHAVE_API',
                'Content-Type': 'application/json'
            },
            json=user_data,
            timeout=30
        )
        
        response.raise_for_status()
        return response.json()
        
    except requests.exceptions.HTTPError as e:
        error_data = e.response.json()
        print(f"Erro da API: {error_data['error']['message']}")
        raise
    except RequestException as e:
        print(f"Falha na solicitação: {str(e)}")
        raise
```

## Códigos de Erro Comuns

| Código | Descrição | Solução |
|--------|-----------|---------|
| `401` | Não autorizado | Verifique sua chave de API |
| `403` | Proibido | Verifique suas permissões |
| `404` | Não encontrado | Verifique a URL do endpoint |
| `422` | Erro de validação | Revise os dados da solicitação |
| `429` | Limite de taxa | Implemente lógica de tentativas |
| `500` | Erro do servidor | Entre em contato com o suporte |

## Limites de Taxa

Nossa API implementa limitação de taxa para garantir uso justo:

- **Nível gratuito**: 1.000 solicitações por hora
- **Nível Pro**: 10.000 solicitações por hora  
- **Empresarial**: Limites personalizados

### Lidando com Limites de Taxa

```javascript
async function fazerSolicitacaoAPI(url, options) {
  const response = await fetch(url, options);
  
  if (response.status === 429) {
    const retryAfter = response.headers.get('Retry-After');
    console.log(`Limite de taxa atingido. Tentar novamente após ${retryAfter} segundos`);
    
    // Aguardar e tentar novamente
    await new Promise(resolve => setTimeout(resolve, retryAfter * 1000));
    return fazerSolicitacaoAPI(url, options);
  }
  
  return response;
}
```

## Próximos Passos

Agora que você completou a configuração básica, explore estes recursos avançados:

1. **[Guia de Autenticação](/docs/authentication)** - Aprenda sobre OAuth 2.0 e tokens JWT
2. **[API de Usuários](/docs/api-reference/users)** - Operações completas de gerenciamento de usuários
3. **[API de Webhooks](/docs/api-reference/webhooks)** - Notificações de eventos em tempo real
4. **[SDKs e Bibliotecas](/docs/sdks)** - Bibliotecas cliente oficiais para linguagens populares

## Exemplos de SDK

Fornecemos SDKs oficiais para linguagens de programação populares:

### JavaScript/Node.js

```bash
npm install @example/identity-sdk
```

```javascript
import { IdentityClient } from '@example/identity-sdk';

const client = new IdentityClient({
  apiKey: 'SUA_CHAVE_API',
  environment: 'production' // ou 'sandbox'
});

const user = await client.users.create({
  email: 'joao.silva@exemplo.com',
  first_name: 'João',
  last_name: 'Silva'
});
```

### Python

```bash
pip install identity-python
```

```python
from identity import IdentityClient

client = IdentityClient(
    api_key='SUA_CHAVE_API',
    environment='production'
)

user = client.users.create(
    email='joao.silva@exemplo.com',
    first_name='João',
    last_name='Silva'
)
```

## Suporte

Precisa de ajuda? Estamos aqui para ajudar:

- **Documentação**: Navegue pela nossa referência completa da API
- **Fórum da Comunidade**: Conecte-se com outros desenvolvedores
- **Suporte por Email**: support@example.com
- **Página de Status**: status.example.com

Feliz codificação! 🚀